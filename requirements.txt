# THETA — Derive funding desk
#
# Only the OPTIONS / PARK path signs Derive actions locally. The perpetual
# hedge is executed by Hummingbot's own derive_perpetual connector and does
# NOT need any of these. Install them to arm the covered-call wing:
#
#   pip install -r requirements.txt
#
# If they are absent, theta_option_ticket and theta_park fail with an
# explanatory error instead of a bare ModuleNotFoundError.
eth-account>=0.11.0
eth-utils>=4.0.0
eth-abi>=5.0.0
